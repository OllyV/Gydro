
# class LineConverter(object):


#     def geometryToLine(geom):
#         dx = layers['dem'].rasterUnitsPerPixelX()
#         dist = np.arange(0, geom.length(), dx)

#         pts = [geom.interpolate(d).asPoint() for d in dist]